import React, { ReactElement } from "react";

const PokeHeader = (): ReactElement => {
  return (
    <header className="border-double text-4xl text-center py-6">
      <h1>Pokédex</h1>
    </header>
  );
};

export default PokeHeader;
